---
alwaysApply: true
description: 项目编码约束，AI 生成代码时必须遵守
---

# 编码约束（Prism / Next.js + Nx）

## 图片

- 禁止使用原生 `<img>` 标签
- 必须使用 `<Image>` from `next/image`，并提供 `width` 和 `height` 属性

```tsx
// ✅ 正确
import Image from 'next/image';
<Image src={url} alt="description" width={48} height={48} />

// ❌ 禁止
<img src={url} alt="description" />
```

## 非空断言

- 禁止使用 `!` 非空断言运算符（`@typescript-eslint/no-non-null-assertion`）
- 改用可选链 `?.` + 空值合并 `?? fallback`

```ts
// ✅ 正确
const url = slide.image?.url ?? '';
const text = slide.image?.alternativeText || defaultText;

// ❌ 禁止
const url = slide.image!.url;
```

## 类型安全

- 禁止使用 `any` 类型（`@typescript-eslint/no-explicit-any`）
- 必须显式声明函数参数和返回值类型（复杂场景）
- 未使用的变量/参数必须以 `_` 前缀命名

```ts
// ✅ 正确
const handler = (_event: MouseEvent) => { ... };
const { unusedProp: _unusedProp, ...rest } = obj;

// ❌ 禁止
const handler = (event: any) => { ... };
```

## 异步处理

- 禁止 floating promise（`@typescript-eslint/no-floating-promises`）
- 异步调用必须 `await`，或用 `void` 明确标记为 fire-and-forget

```ts
// ✅ 正确 — 需要结果时 await
await router.push('/home');

// ✅ 正确 — 明确 fire-and-forget 时用 void
void fetchData();

// ❌ 禁止 — 既不 await 也不 void
router.push('/home');
fetchData();
```

## 导航

- 内部路由跳转使用 `<Link>` from `next/link`，禁止裸 `<a>` 标签
- 外部链接使用 `<a target="_blank" rel="noopener noreferrer">`

```tsx
// ✅ 正确
import Link from 'next/link';
<Link href="/products">Products</Link>

// ❌ 禁止（内部链接）
<a href="/products">Products</a>
```

## 语言

- 所有用户可见的文本（按钮、提示、错误信息、aria-label 等）必须使用**英文**
- 代码注释可以使用中文
