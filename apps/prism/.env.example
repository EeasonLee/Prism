# ============================================
# Prism 应用环境变量配置示例
# ============================================
# 
# 使用说明：
# 1. 复制此文件为 .env.local 或 .env.development
# 2. 根据实际情况修改配置值
# 3. 不要提交包含敏感信息的 .env.local 文件到 Git
#
# ============================================

# ============================================
# 客户端环境变量（NEXT_PUBLIC_*）
# ============================================
# 这些变量会在构建时注入到客户端代码中，可以在浏览器中访问

# 应用的基础 URL
# 用途：生成元数据链接、Open Graph 等
# 格式：必须以 http:// 或 https:// 开头，不要以 / 结尾
# 默认值：http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 后端 API 的基础地址
# 用途：构建 API 请求 URL
# 格式：不要包含 /api 后缀，不要以 / 结尾
# 示例：http://192.168.50.244:1337 或 https://api.example.com
# 注意：如果未设置，某些 API 功能可能无法正常工作
NEXT_PUBLIC_API_URL=http://192.168.50.244:1337

# 图片基础 URL（可选）
# 用途：处理相对路径图片时拼接的前缀
# 格式：必须以 http:// 或 https:// 开头，不要以 / 结尾
# 默认值：
#   - 开发环境：http://localhost:1337
#   - 生产环境：https://d2s2mafqv46idp.cloudfront.net/joydeem/media/pages
# 如果设置了此变量，将优先使用此值
# NEXT_PUBLIC_IMAGE_BASE_URL=http://localhost:1337

# 日志输出级别
# 可选值：debug | info | warn | error
# 默认值：info
# 推荐：
#   - 开发环境：debug（查看详细的 API 请求日志）
#   - 生产环境：error（只记录错误）
NEXT_PUBLIC_LOG_LEVEL=debug

# 是否启用 API 代理
# 用途：如果设置为 true，客户端 API 请求将通过 Next.js 代理转发
# 格式：字符串 "true" 或 "false"，或不设置（默认 false）
# 注意：启用后需要在 next.config.js 中配置相应的 rewrites
# NEXT_PUBLIC_USE_API_PROXY=true

# ============================================
# 服务端环境变量
# ============================================
# 这些变量只在服务端可用，不会暴露到客户端

# Node.js 环境标识
# 可选值：development | production | test
# 注意：通常由 Next.js 自动设置，无需手动配置
#   - pnpm dev → development
#   - pnpm build → production
NODE_ENV=development

# Strapi API 认证 Token（可选）
# 用途：服务端请求后端 API 时使用的认证 Token
# 安全提示：
#   ⚠️ 不要提交到版本控制系统
#   ⚠️ 使用 .env.local 存储（已添加到 .gitignore）
#   ✅ 只在服务端使用，不会暴露到客户端
# STRAPI_API_TOKEN=your_secret_token_here
